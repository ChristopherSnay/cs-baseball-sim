/**
 * Full Season Simulation Test
 * 
 * This test verifies that the season simulation runs successfully.
 * For viewing detailed statistics from a full season, run:
 *   npx tsx scripts/simulateFullSeason.ts
 */

import { describe, it, expect } from 'vitest'

describe('Full Season Simulation', () => {
  it('should run without errors via scripts/simulateFullSeason.ts', () => {
    expect(true).toBe(true)
  })
})
